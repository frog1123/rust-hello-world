mod hello;
mod vars;
mod types;
mod strings;
mod tuples;

// * main file 
// * each mod contains differnt info on stuff
// ? read readme for some cargo commands

fn main() {
  // hello::run();
  // vars::run();
  // types::run();
  // strings::run();
  tuples::run();
}